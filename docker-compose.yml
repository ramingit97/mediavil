services:
  web:
    build: .
    image: mediawil-web
    container_name: mediawil-web
    restart: unless-stopped
    # Bind to localhost only — the host nginx (reverse proxy + SSL) sits in front.
    ports:
      - "127.0.0.1:8080:80"
